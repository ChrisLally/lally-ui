name: Release

on:
  push:
    tags:
      - "ui-branding-v*"

permissions:
  contents: read
  id-token: write

jobs:
  publish-ui-branding:
    permissions:
      contents: read
      id-token: write
    uses: ChrisLally/lally-cli/.github/workflows/release-publish.yml@main
    with:
      tag_prefix: "ui-branding-v"
      package_dir: "packages/ui-branding"
